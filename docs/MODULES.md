# Modules

`src/Plugin.cs` (`DiscoverModules()`) reflects over the assembly at `Awake` and instantiates every
non-abstract subclass of `FeatureModule` (`src/FeatureModule.cs`) it finds — there is no registry
to edit; dropping a new file with such a class is enough. There are exactly 26 such classes. Each
module owns its own Harmony instance, binds its own config section, and declares a `Side` (Server /
Client / Both) — a module whose side doesn't match the running half (dedicated server vs. player
client) is never configured or patched, and shows `disabled(side)` in `nvlb.status`. The table below
adds a 27th row, `Tiers`, for the one piece of shared config that isn't a `FeatureModule` at all
(see the note after the table).

**Enabled-toggle restart rule** (same mechanism for every module — see `FeatureModule.TryEnable` /
`Configure`): `TryEnable()` calls `ApplyPatches()` — which installs the module's Harmony patches —
exactly once, at plugin `Awake`, and only if the module started up `Enabled`. Flipping `[Section]
Enabled` **off** at runtime works live: the Harmony patches stay installed, but every patch body
gates on `Active` (`Applied && Enabled`), so behaviour turns off immediately. Flipping it **on** at
runtime when it started off does **not** take effect until the game/server restarts, because
`ApplyPatches()` is never called again. This is stated once here rather than in every row; the
"Restart for Enabled toggle" column just says `on: restart / off: live` throughout — no module
overrides it (checked: `ExtraSlotsModule`, `VanguardShadowModule`, `DualPowersModule` and
`FistsAndShieldsModule` all do extra cleanup work in `OnConfigChanged`/`Disable` when turned off,
e.g. evacuating stranded items, removing a status effect or restoring the item types they changed,
but none of them re-install patches, so the rule still holds).

**Hot-reload**: `Config.Reload()` (via `ConfigWatcher`, on file-change or a server's `ServerSync`
push) fires `SettingChanged` for every entry bound through `BindSynced`/`BindLocal`, which every
module wires to its own `OnConfigChanged`. In practice this means "yes" for essentially every
setting below — the module re-reads it on the very next use. The table calls out the few modules
where a changed value is visibly deferred (e.g. `ServerKeysModule`, whose keys are re-applied to
the live `ZoneSystem` immediately only if `ApplyOnLoad` is true) or where the setting only gates a
diagnostic that runs once per world load.

| Name | Side | Section | Purpose | Key settings (defaults) | Hot-reload | Restart for Enabled toggle |
|---|---|---|---|---|---|---|
| Status | Client | Status | Registers the `nvlb.status` console command (world tier, catch-up settings, module state); the dedicated server logs the same lines at `ZNet.Start` regardless. | *(no settings of its own)* | n/a | on: restart / off: live |
| Frontier | Both | Frontier | Tracks the world's progress as a tier 0–7 read from the boss global keys (or a forced override); every catch-up module reads `Frontier.WorldTier` / `Tiers.IsBehind`. | `TierOverride=-1` (force a tier for testing, -1=auto); `TiersBehind=1` (how many tiers below the world tier counts as "behind the frontier") | yes | on: restart / off: live |
| ServerKeys | Server | ServerKeys | Applies server-authoritative world modifiers (skill rates, no-cost building/crafting, etc.) via the world's global keys on load, overriding whatever the world file itself stored. | `ApplyOnLoad=true`; `SkillGainRate=1.0`; `SkillReductionRate=1.0`; `NoBuildCost=false`; `NoCraftCost=false`; `AllPiecesUnlocked=false`; `NoWorkbench=false`; `AllRecipesUnlocked=false`; `DeathKeepEquip=false` | yes, but only re-applied live if `ApplyOnLoad=true` and a world is already loaded — otherwise it logs "applied on next world load" | on: restart / off: live |
| PlaytimeRubberBand | Both | Playtime | Gives players with less connected time than the group median a small, capped bonus to gathering and skill XP (server tracks playtime, pushes a per-player factor to clients). Never slows anyone down. | `RecomputeSec=60`; `WindowDays=14`; `MinGroupSize=3` (below this, no bonus); `MaxBonus=1.0` (cap, 1.0=up to 2x); `GatherBonusEnabled=true`; `XpBonusEnabled=true`; local `[Catchup] SelfTest=false` (shared diagnostic flag with GroupSkillCatchup) | yes | on: restart / off: live |
| GroupSkillCatchup | Both | SkillCatchup | Lifts a player's skills that are below the group's best towards it (capped; the leaders themselves get nothing). Clients report skill levels, server computes a group ceiling per skill and broadcasts it. | `ReportSec=60`; `WindowDays=14`; `BroadcastSec=300`; `Bonus=1.0` (strength of catch-up); `MaxFactor=3.0` (hard cap); local `[Catchup] SelfTest=false` (shared) | yes | on: restart / off: live |
| ChestsSelfTest | Both | ChestsSelfTest | Headless diagnostic for CraftFromChests (client-only, so unprovable by a server boot alone): runs the counting/consuming core against its own inventories and logs which prefabs are cooking stations. Changes no real game state. | local `[Chests] SelfTest=false` | value updates immediately, but the test only runs once per world load | on: restart / off: live |
| CraftFromChests | Client | Chests | Crafting, upgrading, building, smelters and fires may pull materials from nearby containers (with a per-player hotkey to toggle it off); the stone oven pulls too, meat racks deliberately do not. | `Range=20` (m); `PullForCrafting=true`; `PullForBuilding=true`; `PullForSmelters=true`; `PullForFires=true`; `PullForCookingStations=false` (**every** CookingStation, racks included — deliberately off, keeps raw meat for recipes); `PullForOvens=true` + `OvenPrefabs="piece_oven"` (the per-prefab exception: a station pulls if EITHER gate allows it); `LeaveOneItem=false`; local `ToggleKey="LeftAlt+O"`. The cauldron (`piece_cauldron`) and CookingAdditions' `BCA_CookingPot` are `CraftingStation`s, not `CookingStation`s — they never reach this gate and are covered by `PullForCrafting`. | yes | on: restart / off: live |
| FistsAndShields | Both | Fists | Fist weapons may be worn with a shield (and with a torch). One field per item prefab in `ObjectDB`: `m_shared.m_itemType` `TwoHandedWeapon` → `OneHandedWeapon` for everything whose `m_skillType` is `Unarmed`, which is what `Humanoid.EquipItem` branches on. Side `Both` only so the dedicated server logs a proof line naming the prefabs it detected — the server itself never equips anything. Nothing serialises the item type (only the prefab name goes into a save/ZDO), so no migration and no ServerSync compatibility concern. | `ExtraPrefabs=""` (extra two-handed weapons to convert; matched on prefab name **or** shared item name); `ExcludePrefabs=""` (leave these two-handed; wins over everything) | yes | on: restart / off: live (the changed types are put back immediately; anyone already holding fists **and** a shield keeps both until they re-equip) |
| FastMining | Client (Both only while `[Mining] SelfTest=true`) | Mining | Ore nodes whose material tier is behind the frontier take pickaxe damage faster, optionally drop more, and optionally ignore the pickaxe's tool-tier requirement. Runs on the node-owning client. | `SpeedMultiplier=3.0`; `DropMultiplier=1.0`; `IgnoreToolTier=false`; `OreNodes="rock4_copper:1,rock4_copper_frac:1,MineRock_Tin:1,silvervein:3,silvervein_frac:3,MineRock_Obsidian:3,MineRock_Meteorite:4"` (the `_frac` entries are the fractured stage a vein is swapped for on the first pickaxe hit - without them only that first hit was fast); local `SelfTest=false` (flips Side to Both for a headless proof) | yes (OreNodes re-resolves the allowlist on next use) | on: restart / off: live |
| WorldSelfTest | Both | World | Headless diagnostic for the two client-only World modules (PortalTrail, LongFires): logs what each WOULD do against the current frontier/ObjectDB/ZNetScene. Changes no game state. | local `SelfTest=false` | value updates immediately, but the test only runs once per world load | on: restart / off: live |
| PortalTrail | Client | Portals | Lets ore/metal behind the frontier travel through portals (vanilla blocks non-teleportable items); only ever relaxes vanilla's rule, never restricts further. | `AllowBehindFrontier=true`; `ExtraTiersBehind=0` (extra margin before ore counts as "behind" for portals specifically); `AlwaysAllow=""`; `NeverAllow=""` (takes priority over everything, including vanilla) | yes | on: restart / off: live |
| LongFires | Client | Fires | Fireplaces (campfires, hearths, torches, braziers, sconces) burn fuel far slower, and hand-held torches lose durability far slower. | `FuelDurationMultiplier=5` (x slower fuel burn); `InfiniteFuel=false`; `HandTorchDurabilityMultiplier=5`; `HandTorchItems="Torch,TorchMist"` | yes (re-applies to every currently-loaded Fireplace/torch item on change) | on: restart / off: live |
| Loadouts | Client | Loadouts | Weapon loadouts: a hotkey swaps to a saved main-hand + off-hand pair, identified by (prefab, quality, variant) rather than a unique item id. | `Slots=2` (server: how many loadouts, 0–4); local `Loadout1Key="V"`; `Loadout2Key="B"`; `SaveModifier="LeftControl"` (hold + press a loadout key to save instead of equip) | yes | on: restart / off: live |
| FoodNoDecay | Client (Both only while `[Food] SelfTest=true`) | Food | Eaten food keeps its full health/stamina/eitr contribution until it expires, instead of fading toward the end of its timer (a transpiler replaces vanilla's decay curve). Also stops the HUD icons pulsing. | `KeepFraction=1.0` (floor on the decayed fraction; 1.0=no decay at all); `CurveExponent=0.3` (vanilla's own curve, below the floor); `HidePulse=true`; `PulseBelowSeconds=0` (0=never re-enable the vanilla "about to run out" pulse); local `SelfTest=false` | yes | on: restart / off: live |
| SlotsSelfTest | Both | SlotsSelfTest | Headless proof for ExtraSlots and Loadouts' storage layer (save lift, blob round-trip, migration rescue from shudnal's ExtraSlots, loadout encoding) — provable on a dedicated server even though both features are client-only. | local `[Slots] SelfTest=false` (bound in the `[Slots]` section, next to ExtraSlots) | value updates immediately, but the test only runs once per world load | on: restart / off: live |
| ExtraSlots | Client | Slots | Extra inventory slots — equipment (head/chest/legs/cape), utility, food, ammo and plain storage — as real cells of the player's own inventory grid, never written into the vanilla save package (so a vanilla client/tool can't delete them); also rescues characters migrating from shudnal's ExtraSlots. | `EquipmentSlots=true`; `UtilitySlots=2` (0–4); `FoodSlots=3` (0–3); `AmmoSlots=2` (0–4); `QuickSlots=0` (0–8, hotkeyed); `GenericSlots=2` (0–8, plain storage); `AutoEatFromFoodSlots=true`; local `QuickSlotKeys="Z,X,C"`; `ShowUI=true` | yes (layout changes relayout every item live) | on: restart / off: live |
| VanguardShadow | Client (Both only while `[Vanguard] SelfTest=true`) | Vanguard | A player near a better-geared ally gets "Vanguard's Shadow": less incoming damage, more skill XP, faster stamina regen — only helps whoever is behind, only while near the person ahead. Gear tier is published on the player's own ZDO (no custom RPC needed). | `Radius=20` (m); `TierGap=1` (how many gear tiers ahead the other player must be); `UpdateSec=5`; `DamageReduction=0.25`; `XpBonus=0.5`; `StaminaRegen=0.2`; `RequireBehindFrontier=false`; local `SelfTest=false` | yes | on: restart / off: live |
| EconomySelfTest | Both | Economy | Headless diagnostic for the three client-only Economy modules (TrailingTierDiscount, RichSmelting, TraderStock): logs what each WOULD do against the current frontier/ObjectDB. Changes no game state. | local `SelfTest=false` | value updates immediately, but the test only runs once per world load | on: restart / off: live |
| TrailingTierDiscount | Client | Discount | Recipes and build pieces whose tier is behind the frontier cost less to craft/build; the recipe's tier is the max tier over its own requirements, so cheap components of an otherwise-behind recipe are discounted too. | `CostMultiplier=0.5` (fraction of vanilla cost, clamped to ≤1 — never makes things pricier); `ExtraPerTierBehind=0.0` (extra discount per further tier behind); `MinAmount=1` (floor — cost never drops to zero) | yes | on: restart / off: live |
| TraderStock | Client | Trader | Haldor (and optionally other traders) also sells bars of trailing-tier metals, gated by vanilla's own boss-key requirement so a metal only appears once it is actually behind the frontier. | `Items="Bronze:5:60,Iron:5:80,Silver:5:120,BlackMetal:5:150"` (PrefabName:stack:price); `TraderNames="Haldor"` (comma-separated, `*`=every trader) | yes, but only for traders spawned after the change — an already-loaded trader keeps its list until it respawns | on: restart / off: live |
| RichSmelting | Client | Smelting | Trailing-tier metal comes out richer: smelters/blast furnaces/kilns whose OUTPUT is behind the frontier spawn extra items per input, and crafting recipes whose output is such a material yield more per craft. | `OutputMultiplier=2` (smelter items per input); `RecipeYieldMultiplier=2` (craft yield) | yes | on: restart / off: live |
| CorpseRunPlus | Client (Both only while `[CorpseRun] SelfTest=true`) | CorpseRun | Five independent QoL features for the run back to your corpse, each with its own sub-toggle: an off-screen-waypoint HUD compass to the grave (edge marker, hold a key to dismiss it), one free food item and a guaranteed Rested buff on death-respawn, a distance-scaled stamina buff while the corpse is far away, and stretching vanilla's CorpseRun loot buff by how far the grave was from home. | `CompassEnabled=true` (`CompassHideDistance=10m`; local `CompassMode="Edge"`/`Fixed`, `CompassEdgeMargin=60px`, `ClearGraveKey="Delete"`, `ClearGraveHoldSec=1.5`); `RespawnFoodEnabled=true` (`RespawnFoods="Bread"`, `RespawnFoodCount=1`); `RespawnRestedEnabled=true` (`RestedMinutes=10`); `PullEnabled=true` (`PullMinDistance=50m`, `PullFullDistance=1000m`, `PullMaxRegenBonus=1.0`, `PullMaxDrainReduction=0.5`); `ScaledEnabled=true` (`ScaledDurationPer100m=0.2`, `ScaledMaxDurationSec=900`); local `SelfTest=false` | yes | on: restart / off: live |
| OreRegrowth | Server | Regrowth | Mined-out ore nodes whose material tier is behind the frontier come back after N in-game days. Server-only: destruction is recorded and respawn ZDOs are created entirely server-side, persisted to `nvlb/regrowth.json`. A dedupe guard refuses to respawn when a node of that prefab (or its fractured stage) already stands within 4 m. | `Prefabs="rock4_copper_frac:1:rock4_copper,silvervein_frac:3:silvervein,MineRock_Tin:1,MineRock_Obsidian:3,MineRock_Meteorite:4"` (`recordPrefab:tier[:respawnPrefab]` - copper/silver are RECORDED on their fractured stage, which only dies when the vein is fully mined, and the ORIGINAL vein is what comes back; the old `name:tier` form is migrated automatically); `RegrowDays=7`; `CheckIntervalSec=60`; `MinPlayerDistance=64m` (never respawn with a player this close); `MaxPerTick=5`; local `DryRun=false`; local `SelfTest=false` | yes (Prefabs re-resolves the allowlist on next sweep) | on: restart / off: live |
| CombatRecharge | Client | Recharge | Landing or taking a hit shaves seconds off your Forsaken power cooldown(s), rate-capped per real second so multi-hit AoE/DoT can't dump a cooldown instantly. Composes with DualPowers' extra slots, and every second it removes is visible as the charge ring on the power icon jumping forward (DualPowers' `[Powers] CooldownRing`, since 0.4.8). | `SecondsPerHitDealt=2`; `SecondsPerHitTaken=3`; `MaxPerSecond=10` (hard cap on reduction per real second); `AffectAllSlots=true` (also shorten DualPowers' extra slots); `CountPlayerTargets=false` | yes | on: restart / off: live |
| DualPowers | Client (Both only while `[Powers] SelfTest=true`) | Powers | Carry two (optionally three) Forsaken powers at once, each with its own cooldown; routes the boss-stone altar to the first empty slot (or replaces the last) while holding `Slot1Modifier` while interacting replaces slot 1, names the slot and its key in a centre message, writes the key on the second HUD icon, and adds a second/third activation hotkey. `nvlb.power clear 1|2` and `nvlb.power swap` (never gated by `[Debug] AllowTestCommands`) fix a power in the wrong slot. Also owns the **power HUD decoration** (0.4.8, `src/Modules/Powers/PowerRing.cs`): every power icon — vanilla's slot 1 as well as the cloned extra slots — is masked to a circle and given a thin cooldown/charge ring (`fillAmount = 1 - remaining / StatusEffect.m_cooldown × CooldownMultiplier`), so recovery is visible; both sprites are generated at runtime (256×256 `Texture2D`, no shipped assets) and the vanilla icon is only *moved* inside a generated `Mask`, never replaced, and is restored exactly on teardown. The decoration applies even at `Slots=1` or `ShowHud=false`, but only while this module is enabled. | `Slots=2` (1–3); `IndependentCooldowns=true` (false=one shared cooldown); `CooldownMultiplier=1.0`; local `SecondSlotKey="G"`; `ThirdSlotKey="None"`; local `Slot1Modifier="LeftShift"` (hold while interacting with an altar to fill slot 1); `ShowHud=true`; local `HudOffsetX=0`/`HudOffsetY=-56`; local **`RoundIcons=true`**; **`CooldownRing=true`**; **`RingThickness=4`** (px, 1–24); **`RingColor="E6C88AD9"`** (RGBA hex); **`RingTrackColor="00000066"`**; local `SelfTest=false` (also proves the ring's disc/annulus coverage maths and sprite generation headlessly) | yes (a colour change repaints in place; a geometry/toggle change rebuilds the decoration on the next frame) | on: restart / off: live |
| TestCommands | Client | Debug | Admin-only test helpers for a private server: `nvlb.give <ItemPrefab> [amount] [quality]`, `nvlb.power <list\|GP_Name> [1\|2]` and `nvlb.tier`, plus the ungated `nvlb.power clear <1\|2>` and `nvlb.power swap` (they only move powers you already earned between slots, so they work even with `AllowTestCommands=false`). They exist because a dedicated server refuses vanilla cheat commands for clients (`Terminal.IsCheatsEnabled()` needs `ZNet.IsServer()`), so client-side features like DualPowers could not be tested from a connected client. Everything acts on the LOCAL player only - inventory and guardian powers are client-side state, no RPC and no world edits. | `AllowTestCommands=false` (**server-synced master switch**; while false every command prints "disabled by server") | yes (the flag is read per command) | on: restart / off: live |
| Tiers | n/a (shared config, not a `FeatureModule`) | Tiers | Holds the material → tier map every catch-up/economy module keys off (`Tiers.IsBehind`, `Tiers.OfItem`, etc.). Bound directly from `Plugin.Awake` via `Tiers.BindConfig()`, before module discovery runs, because several modules read it while binding their own config. | `MaterialTiers="..."` (comma-separated `PrefabName:tier` pairs, default covering tiers 1–6: Copper/Tin/Bronze… through FlametalNew/Grausten/MoltenCore…; anything unlisted is tier 0, never "behind the frontier") | yes (re-parsed and re-validated against ObjectDB on change) | n/a — always active, no `Enabled` toggle |

`Tiers` (`src/Tiers.cs`, `internal static class Tiers`) is the 27th entry above but is plugin-level
shared configuration, not a `FeatureModule` subclass: it has no `Side` and no `[Section] Enabled`
toggle of its own, and is not independently toggleable the way the other 26 modules are — it is
simply always bound and always active.
